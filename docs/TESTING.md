# Testes

O CI executa duas validações mínimas:

1. import/parsing em modo editor headless;
2. smoke test da cena principal por alguns frames.

Falhas de parser ou runtime devem ser corrigidas antes do merge em `main`.
